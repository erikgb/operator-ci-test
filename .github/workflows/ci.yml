---
name: CI
on:
  pull_request: {}
  push:
    branches:
      - main
permissions:
  contents: read
jobs:
  verify-generated:
    runs-on: ubuntu-latest
    steps:
      - name: Generate all
        run: |
          make generate-all
